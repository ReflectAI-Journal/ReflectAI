import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.reflectai.journal',
  appName: 'ReflectAI',
  webDir: 'dist',  // Points to your build output directory
};

export default config;
